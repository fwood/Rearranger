function simplebound(A, V, zeta, gamma, rho, xi, b, a, alpha, epsilon)
% function simplebound(A, V, zeta, gamma, rho, xi, b, a, alpha, epsilon)
% 
% Variational bound for log p(A | a, alpha, V, epsilon).

